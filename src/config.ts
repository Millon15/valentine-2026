/**
 * Valentine Quiz Configuration
 *
 * Change the values below to customize the quiz for your own use.
 * No other source files need to be modified.
 */
export const config = {
  /** The recipient's name (the person taking the quiz) */
  recipientName: 'Tanya',

  /** The sender's name (the person who created the quiz) */
  senderName: 'Vitas',
};
